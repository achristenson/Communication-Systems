finalProject

close all

sum = zeros(1,length(b)*T+2*K*T);

amp = 2*b-1;

for i=0:length(b)-1

    y = zeros(1,length(b)*T+2*K*T);

    y(1+i*T:2*K*T+i*T) = amp(i+1)*pulse_SRRC;
    
    sum = sum + y;
end

figure
